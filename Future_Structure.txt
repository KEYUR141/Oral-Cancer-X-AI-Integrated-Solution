Oral-Cancer-X-AI-Integrated-Solution/
│
├── data/
│   ├── raw/                          # untouched API responses, timestamped
│   │   └── papers_semantic_scholar_2024-06.json
│   ├── processed/                    # after clean_papers.py + finalize_dataset.py
│   │   └── papers_final.json
│   ├── embeddings/                   # output of embed_papers.py (text + vectors)
│   │   └── papers_embedded.json
│   │
│   ├── pdfs/                         # PHASE 2 — full-text PDFs (not yet)
│   │   ├── raw/                      #   downloaded PDF files, named by paperId
│   │   └── extracted_text/           #   parsed section-wise text (JSON per paper)
│   │
│   ├── images/                       # PHASE 4 — figures extracted from papers (not yet)
│   │   ├── raw/                      #   extracted figure images, named by paperId_figN
│   │   └── descriptions/             #   Gemini-Vision-generated captions (JSON)
│   │
│   └── references/                   # PHASE 3 — citation graph expansion (not yet)
│       └── expanded_papers.json
│
├── ingestion/                        # scripts that PRODUCE data/ files
│   ├── fetch_papers.py               # (your fetch_research.py, renamed for clarity)
│   ├── clean_papers.py               # (existing)
│   ├── finalize_dataset.py           # (existing)
│   ├── fetch_pdfs.py                 # PHASE 2 (not yet)
│   ├── extract_pdf_text.py           # PHASE 2 (not yet)
│   ├── extract_figures.py            # PHASE 4 (not yet)
│   └── expand_references.py          # PHASE 3 (not yet)
│
├── embeddings/
│   ├── embed_papers.py               # title+abstract → vectors (current)
│   └── embed_fulltext.py             # PHASE 2 — section-chunked embeddings (not yet)
│
├── db/
│   ├── schema.sql                    # CREATE TABLE statements, pgvector setup
│   ├── load_to_postgres.py           # reads embeddings JSON → inserts rows
│   └── migrations/                   # schema changes over time (alembic-style, optional)
│
├── app/                              # the actual RAG backend (FastAPI)
│   ├── main.py                       # API entrypoint
│   ├── retrieval.py                  # pgvector similarity search logic
│   ├── llm.py                        # Gemini prompt construction + call
│   └── config.py                     # env vars, DB connection string, model names
│
├── Resouces&Research/                # your existing research notes — keep as-is
│   ├── Research/
│   │   └── Chatgpt.md / Chatgpt.txt
│   └── Resources/
│       └── resouces_list.txt
│
├── requirements.txt
├── .env.example                      # DATABASE_URL, GEMINI_API_KEY placeholders (never commit real .env)
├── .gitignore
└── README.md